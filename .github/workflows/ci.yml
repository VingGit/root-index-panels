name: CI

on:
  pull_request:

jobs:
  diagnose-real-quartz-integration:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout synthetic merge
        uses: actions/checkout@v6

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: "22"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Checkout Quartz integration host
        run: |
          git clone --depth 1 https://gitlab.utu.fi/obsidian/quartz-for-gitlab.git "$RUNNER_TEMP/quartz-for-gitlab"
          echo "RIP_QUARTZ_ROOT=$RUNNER_TEMP/quartz-for-gitlab" >> "$GITHUB_ENV"
          git -C "$RUNNER_TEMP/quartz-for-gitlab" rev-parse HEAD > quartz-host-revision.txt

      - name: Run real Quartz integration diagnostic
        id: integration
        continue-on-error: true
        run: |
          set +e
          npm run test:integration > real-quartz-integration.log 2>&1
          status=$?
          printf '%s\n' "$status" > real-quartz-integration.status
          tail -n 80 real-quartz-integration.log
          exit "$status"

      - name: Upload real Quartz integration diagnostic
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: real-quartz-integration-log
          path: |
            real-quartz-integration.log
            real-quartz-integration.status
            quartz-host-revision.txt
